# Brick 4 AMU dependencies. The sensor libraries MUST be installed into the
# same interpreter the service runs as (never --user) or the service fails with
# ModuleNotFoundError: No module named 'board'. The installer enforces this.
# wolfssl replaces python3-dtls: only wolfssl speaks DTLS 1.3, and 1.2 is
# the version that panics the NMU. zeroconf finds the server by name so the
# address is never hardcoded.
#
# wolfssl is PINNED: PyPI ships it source-only, so every install compiles the
# bundled C library and a newer release's API change would only surface after
# deployment. 5.9.2.post0 is the soaked and wire-observed version
# (evidence/2026-08-29_amu_cipher_observed.md).
wolfssl==5.9.2.post0
zeroconf>=0.130
adafruit-blinka
adafruit-circuitpython-dht
smbus2
pimoroni-bme280
ltr559
pms5003
scd30-i2c
